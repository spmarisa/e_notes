# Erlang module timer is called just that, the atom timer . In Elixir we write that as :timer . If you want to refer to the tc function in timer , you’d write :timer.tc


:io.format("The number is ~3.1f~n", [5.678])
