in elixir is not an assignment operator, its a match operator

a = 1 + 1
we are telling elixir to match left hand side against the expression on right
here elixir will store 2 in a because a currently has no value 
